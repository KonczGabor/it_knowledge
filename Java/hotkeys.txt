Ctrl+Shift+L akutális gyors gombok listája
Next editor Ctrl+F6 helyette ctrl+tab vissza meg ctrl+shift+tab

Ctrl-m ablak minimalizálása.

Ctrl+shift+r : open resource csak java filokat keres

Ctrl+shift+t: java osztályokat keres.

Ctrl+o: az adott osztályban gyors keresés a metódusok és mezõk között.

alt+shift+r : rename the local variable in everywhere.

kijelölni mindent (ctrl+a) majd ctrl+1: minden nem használt final lesz.

ctrl+7 ki és visszakomment.

ctrl+shift+i => inspect a részutasításra.

ctrl+3: display

jobbklikk az osztályon -> open type hierarchy

jobklikk a metóduson -> open call hierarchy 

pull up, push down a refaktorban õsbe, gyerekbe áthelyezi az adott változót. Lehet mást is tud. 

ctr+o: quick outline. Ha kétszer nyomod meg akkor azokat is mutatja, amiket õ örököl.

easyshell plugin a marketplacebõl a package explorerben oda nyit ablakot ahol jobklikkelsz.

ctrl+1 az adott változón és convert local variable to field hogy statikus legyen.

A fordítást a projekt gyökerébõl kell kiadni.

Az Eclipse-ben a clean gomb arra jó hogy ha elcsúszik a forrásfiletól a lebuildelt, akkor azokat kitörli és újra buildel. Olyankor lehet amikor utólag kerülnek be dependency-k. Néha segít.

Degug: CTRL+3: odaugrik a QuickAcces-re