Két ugyanolyan elem nem kerülhet bele.

Nem redndezett, nem tudom kivenni get(0) -val az adott elemet.

halmaz.contains(adottElem) => true, false 

halmaz.put(key, value);

halmaz.keySet()//values//entry visszaadja az összes kuclcsot, foreach esetén hasznos a  bejárásnál

Ugyanazt az elemet csak egyszer lehet hozzáadni egy sethez.

ConcurrentSkipListSet arra való hogy foreach-es bejárás során módosításokat hajthassunk végre azon.

