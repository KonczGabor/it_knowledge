System.out.println(Array.toString(tomb));  //Királyul kiírja
méret:
	myArray.length;
	
A méretet és a tartalmazott elemek listáját nem szabad egyszerre meagdni. Vagy/Vagy. 
Az üres {} is elfogadott:
	String lion [] = new String[] {"lion"};
	String tiger [] = new String[1] {"tiger"};		//Nem jó
	String bear [] = new String[] {};
	String ohMy [] = new String[0] {};				//Nem jó
	
binarySearch() method requires a sorted array in order to return a correct
result. If the array is not sorted, the results of a binary search are undefined.

Ez a deklaráció mind megengedett:
	String[][] gamma;
	String[] delta[];
	String epsilon[][];
	
Magán a tömbön hívott .toString() nem írja ki a tömb elemeit, ahhoz az Arrays.toString(tömb) kéne.

A Arrays.binarySearch(hol, mit) negatív számot ad vissza, ha nincs benne az elem a keresett listában, 
megmondja h hanyadik helyen kéne lennie, negálja és még hozzá ad -1 et.
Tehát ha valaminek a 6. helyen lenne a helye, akkor -6 lesz a viszzatérési érték.

Array.toString(myEmptyArray) 	// => []

Ha nem sort() -olunk egy tömböt mielõtt használnák rajta a binarySearch()-öt, akkor az eredmény undefinied lesz.
Hiába van jó indexen alapból a keresett elem, a válasz az undefinied.

Üres tömbön hívott myUresTomb[0]	// => AIOOBE
